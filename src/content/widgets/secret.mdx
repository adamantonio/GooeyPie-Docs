---
title: Secret

syntax: my_secret = gp.Secret(placeholder_text)

parameters:
  - name: placeholder_text
    type: string
    description: '<strong>Optional.</strong> The text that will be displayed in the Secret when it is empty. This text disappears when the Secret gains focus.'

properties:
  - name: text
    type: string
    description: Gets or sets the text of the secret, even if it is currently masked. When setting this property, the text will be masked or unmasked based on the current state of the secret.
  - name: placeholder_text
    type: string
    description: Gets or sets the placeholder text of the entry.
  - name: masked
    type: boolean
    description: Gets whether the text in the secret is currently masked (displayed as dots) or unmasked (displayed as actual characters). This property is read-only; to change the masked state, use the `mask()`, `unmask()`, or `toggle()` methods.
  - name: width
    type: integer
    description: Gets or sets the width of the entry in pixels.
  - name: height
    type: integer
    description: Gets or sets the height of the entry in pixels.
  - name: disabled
    type: boolean
    description: Gets or sets whether the entry is disabled. When disabled is set to `True`, the entry does not respond to user input.

methods:
  - name: unmask()
    parameters: none
    returns: None
    description: Unmasks the text in the secret, showing the actual characters instead of dots (•).
  - name: mask()
    parameters: none
    returns: None
    description: Masks the text in the secret, replacing each character with a dot (•). This is the default behavior of the Secret widget.
  - name: toggle()
    parameters: none
    returns: None
    description: Toggles between masked and unmasked states of the secret.
  - name: clear()
    parameters: none
    returns: None
    description: Clears the text in the entry.
  - name: focus()
    parameters: none
    returns: None
    description: Sets the focus to the entry, allowing the user to start typing in it immediately.
  - name: select()
    parameters: none
    returns: None
    description: Selects all the text in the entry.

events:
  - name: on_change(event_function)
    description: The function to call when the text in the entry changes. This event is triggered whenever the user types in the entry or when the text is changed programmatically.

styles:
  - name: bg_color
    description: The background color of the entry.
  - name: border_color
    description: The color of the border of the entry.
  - name: border_width
    description: The width of the border of the entry.
  - name: corner_radius
    description: The radius of the corners of the entry.
    default: "6"
  - name: font_name
    description: 'The font of the text of the Entry. Either an installed font or a generic font name: `"serif"`, `"sans-serif"`, `"monospace"` or `"system"`'
    default: '"system"'
  - name: font_size
    description: The size of the text of the Entry.
    default: "12"
  - name: font_weight
    description: 'The weight of the text of the Entry. Either `"normal"` or `"bold"`.'
    default: '"normal"'
  - name: font_style
    description: 'The style of the text of the Entry. Either `"normal"` or `"italic"`.'
    default: '"normal"'
  - name: justify
    description: 'The horizontal alignment of the text in the entry. Can be set to `"left"`, `"center"`, or `"right"`.'
    default: '"left"'
  - name: text_color
    description: The color of the text of the entry.
  - name: text_disabled_color
    description: The color of the text of the entry when it is disabled.
  - name: placeholder_color
    description: The color of the placeholder text of the entry.
  
---

import CodeExample from '../../components/CodeExample.astro';


A Secret widget is a type of entry widget that is designed for entering sensitive information, such as passwords. By default, the text entered into a Secret widget is masked, meaning that it is displayed as dots (•) instead of the actual characters.

## Examples

### Login form
Here's an example of a simple login form with username and password fields. The `.text` property is used to get the text in both the regulary Entry and the Secret widgets.

<CodeExample
  name="login_form"
  title="Login form"
  mark="user_entry"
/>

### Tell me a secret
In this example, the `toggle()` method of the Secret widget is used to show or hide the message.

<CodeExample
  name="my_secret"
  title="Tell me a secret"
  mark="secret_chk"
/>
